#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: first input
 * @b: second input
 * Return: multiplication results ot the 2 integers
 */
int mul(int a, int b)
{
	return (a * b);
}
